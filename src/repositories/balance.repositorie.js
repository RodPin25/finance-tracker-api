//Para poder hacer el balance
async function getTransactions(userID){
    console.log("[INFO] Fetching transactions for user: ",userID);
    //Logica para conexion a base de datos
}

module.exports = { getTransactions }